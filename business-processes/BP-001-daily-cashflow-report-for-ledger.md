# Daily Cashflow Report for Ledger — `BP-001`

**Status:** `Not Implemented` — example entry only, seeded to show this
template's shape; no `REQ-` exists for it yet. Turn it into a real
requirement the normal way — `/new-requirement`, or manually per
`docs/03-spec-driven-development-playbook.md` §6 — when it's actually
needed, then update this file's Status and Related Requirements, and the
index row in `docs/06-business-processes-and-domain-glossary.md` §3.

**Date:** 2026-09-20
**Owned by:** Product Owner

---

**Owner / stakeholder:** `<who in the business asked for or consumes
this — e.g. the Finance/Treasury team>`

**Trigger:** Scheduled — once per business day, after end-of-day batch
close.

**Frequency:** Daily.

**Actors:**
- `<the system/job that triggers it — e.g. a nightly scheduler>`
- `<the consumer — e.g. the ledger/accounting system that ingests the
  report, or a finance analyst who reviews it>`

**Inputs:** `<e.g. the day's posted ledger transactions, the active
cashflow schedules generated by this service (FR-1), the prior day's
closing balances>`

**Outputs:** `<e.g. a report — file or API payload — listing that day's
expected cashflows (interest/principal due, by instrument), reconciled
against what the ledger actually posted>`

**Business rules:**
- `<e.g. only instruments active as of the report date are included>`
- `<e.g. a discrepancy between an expected and a posted cashflow is
  flagged on the report, never silently dropped>`
- `<name every rule explicitly here — this is exactly the kind of thing
  that otherwise gets improvised mid-implementation with no record of
  why it works the way it does>`

**Workflow:**
1. `<e.g. end-of-day batch close completes and emits a signal>`
2. `<e.g. the report job pulls that day's active schedules and posted
   ledger entries>`
3. `<e.g. it reconciles the two and produces the report, flagging
   discrepancies>`
4. `<e.g. the report is delivered to its consumer(s)>`

**Related requirements:** None yet (`Not Implemented`). A real spec for
this should build on `FR-1`'s existing `CashflowScheduleGenerator`
(`docs/02-techlead-design.md`) rather than a new engine, and should name
that reuse explicitly per
`docs/03-spec-driven-development-playbook.md` §6 step 2.

**Glossary terms used:** *Ledger*, *Cashflow Schedule*, *Reconciliation*
(`docs/06-business-processes-and-domain-glossary.md` §4).
